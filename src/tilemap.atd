type tilemap = {
  layers: layer list;
  height: int;
  width: int;
  tilesets: tileset list;
}

type layer = {
  data: int list;
}

type tileset = {
  firstgid: int;
}

type tilesheet = {
  tileheight: int;
  tilewidth: int;
  columns: int;
}
